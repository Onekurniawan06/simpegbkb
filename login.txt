<!-- START: Kode Notifikasi Sukses (Posisi tengah melayang/modal) -->
        @if (session('status'))
            <div id="success-popup" class="fixed inset-0 bg-black/75 flex items-center justify-center z-50 p-4">
                <div class="bg-green-900 p-6 rounded-lg shadow-xl max-w-sm w-full text-white relative">
                    <div class="flex items-start">
                        <svg class="h-6 w-6 text-green-400 mr-4 flex-shrink-0" xmlns="www.w3.org" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <div class="flex-grow">
                            <h4 class="text-lg font-semibold">Registrasi Berhasil!</h4>
                            <p class="mt-1 text-sm text-green-100">{{ session('status') }}</p>
                            <div class="mt-4">
                                <button onclick="closeSuccessPopup()" class="px-4 py-2 bg-green-700 text-white text-sm font-medium rounded-lg shadow-sm hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 focus:ring-offset-green-900">
                                    Tutup
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        @endif
        <!-- END: Kode Notifikasi Popup Sukses -->

        <!-- START: Kode Notifikasi Error (Posisi tengah melayang/modal) -->
        @if ($errors->any())
            <div id="error-popup" class="fixed inset-0 bg-black/75 flex items-center justify-center z-50 p-4">
                <div class="bg-red-900 p-6 rounded-lg shadow-xl max-w-sm w-full text-white relative">
                    <div class="flex items-start">
                        <svg class="h-6 w-6 text-red-400 mr-4 flex-shrink-0" xmlns="www.w3.org" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <div class="flex-grow">
                            <h4 class="text-lg font-semibold">Terjadi Kesalahan!</h4>
                            <ul class="mt-1 list-disc list-inside text-sm text-red-100 space-y-1">
                                @foreach ($errors->all() as $error)
                                    @php
                                        // Secara manual terjemahkan pesan error yang umum:
                                        $translatedError = $error;
                                        if (Str::contains($error, 'The name field is required.')) {
                                            $translatedError = 'Kolom nama lengkap wajib diisi.';
                                        } elseif (Str::contains($error, 'The email field is required.')) {
                                            $translatedError = 'Kolom alamat email wajib diisi.';
                                        } elseif (Str::contains($error, 'The email must be a valid email address.')) {
                                            $translatedError = 'Alamat email harus valid.';
                                        } elseif (Str::contains($error, 'The password field is required.')) {
                                            $translatedError = 'Kolom password wajib diisi.';
                                        } elseif (Str::contains($error, 'The password field confirmation does not match.')) {
                                            $translatedError = 'Konfirmasi password tidak cocok.';
                                        } elseif (Str::contains($error, 'The email has already been taken.')) {
                                            $translatedError = 'Email sudah terdaftar.';
                                        } elseif (Str::contains($error, 'The password field must be at least 8 characters.')) {
                                            $translatedError = 'Kata sandi minimal 8 karakter.';
                                        }
                                    @endphp
                                    <li>{{ $translatedError }}</li>
                                @endforeach
                            </ul>
                            <div class="mt-4">
                                <button onclick="closeErrorPopup()" class="px-4 py-2 bg-red-700 text-white text-sm font-medium rounded-lg shadow-sm hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 focus:ring-offset-red-900">
                                    Tutup
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        @endif
        <!-- END: Kode Notifikasi Error -->